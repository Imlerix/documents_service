const common = require('./commonErrors');
const doctype = require('./doctypeErrors');
const document = require('./documentErrors');
const extraField = require('./extraFieldErrors');

module.exports = {
    common,
    doctype,
    document,
    extraField
}
